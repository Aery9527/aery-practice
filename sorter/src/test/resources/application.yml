
sorter:
  date:
    key: timestamp
    format: yyyy-MM-dd'T'HH:mm:ss.SSS'Z'
  provider:
    csv-file:
      location: data.csv
  exporter:
    csv-file:
      location: sort.csv
      sort-keys: ${sorter.date.key}, level, msg
